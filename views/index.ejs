<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Notes App</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10 text-white">
            <form action="/create" method="post">
                <input class="block rounded-md outline-none w-full px-5 py-5 bg-zinc-600" type="text" placeholder="Title goes here..." name="title">
                <textarea class="block w-full h-100 rounded-md outline-none mt-4 px-5 py-5 bg-zinc-600 resize-none placeholder:text-center" placeholder="Write your Task detail.." name="details"></textarea>
                <input class="mt-3 bg-blue-600 px-5 py-2" type="submit" value="Create Task">
            </form>
        </div>

        <div class="tasks p-10 flex gap-6 flex-wrap">
            <% if(files.length > 0) { %>
                <% files.forEach(file => { %>
                <div class="task w-72 px-3 py-4 rounded-md bg-zinc-700">
                <h1 class="text-white text-3xl tracking-tighter"><%= file %></h1>
                <a class="text-blue-400 inline-block mt-3 hover:text-blue-300" href="/files/<%= file %>">View Details...</a>
                <a href="/edit/<%= file %>" class="text-zinc-400 inline-block mt-3 px-7 hover:text-zinc-300">Edit Name</a>
                </div>
                <% })%>
            <%} else {%>
                <h3 class="text-zinc-300">no task yet..</h3>
            <%}%>
        </div>
    </div>
</body>
</html>